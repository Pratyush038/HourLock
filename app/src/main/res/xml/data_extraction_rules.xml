<?xml version="1.0" encoding="utf-8"?>
<!--
    data_extraction_rules.xml
    Prevents any HourLock data from being backed up to Google Drive or
    transferred via device-to-device migration. All usage data is local-only
    and should not follow the user to a new device (it would reflect a
    different device's usage, not theirs).
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="sharedpref" path="." />
        <exclude domain="database" path="." />
        <exclude domain="file" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="sharedpref" path="." />
    </device-transfer>
</data-extraction-rules>
